import { async, ComponentFixture, TestBed } from '@angular/core/testing';

/* TODO corrigir teste ListagemInstrumentosContratuaisComponent
describe('ListagemInstrumentosContratuaisComponent', () => {
  let component: ListagemInstrumentosContratuaisComponent;
  let fixture: ComponentFixture<ListagemInstrumentosContratuaisComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListagemInstrumentosContratuaisComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ListagemInstrumentosContratuaisComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
