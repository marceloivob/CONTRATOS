import { async, ComponentFixture, TestBed } from '@angular/core/testing';

/* TODO corrigir teste EditarInstrumentosContratuaisComponent
describe('EditarInstrumentosContratuaisComponent', () => {
  let component: EditarInstrumentosContratuaisComponent;
  let fixture: ComponentFixture<EditarInstrumentosContratuaisComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EditarInstrumentosContratuaisComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(EditarInstrumentosContratuaisComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */