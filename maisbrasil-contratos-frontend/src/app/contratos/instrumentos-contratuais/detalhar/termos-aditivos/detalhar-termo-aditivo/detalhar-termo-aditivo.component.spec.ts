import { async, ComponentFixture, TestBed } from '@angular/core/testing';
/*
import { DetalharTermoAditivoComponent } from './detalhar-termo-aditivo.component';

describe('DetalharTermoAditivoComponent', () => {
  let component: DetalharTermoAditivoComponent;
  let fixture: ComponentFixture<DetalharTermoAditivoComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DetalharTermoAditivoComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DetalharTermoAditivoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
