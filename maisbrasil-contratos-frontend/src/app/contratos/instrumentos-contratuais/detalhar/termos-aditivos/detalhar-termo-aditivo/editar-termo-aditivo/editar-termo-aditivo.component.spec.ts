import { async, ComponentFixture, TestBed } from '@angular/core/testing';

/*
import { EditarTermoAditivoComponent } from './editar-termo-aditivo.component';

describe('EditarTermoAditivoComponent', () => {
  let component: EditarTermoAditivoComponent;
  let fixture: ComponentFixture<EditarTermoAditivoComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EditarTermoAditivoComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(EditarTermoAditivoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
