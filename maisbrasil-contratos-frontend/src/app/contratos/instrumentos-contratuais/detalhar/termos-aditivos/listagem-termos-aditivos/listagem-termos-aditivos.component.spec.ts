import { async, ComponentFixture, TestBed } from '@angular/core/testing';
/*
import { ListagemTermosAditivosComponent } from './listagem-termos-aditivos.component';

describe('ListagemTermosAditivosComponent', () => {
  let component: ListagemTermosAditivosComponent;
  let fixture: ComponentFixture<ListagemTermosAditivosComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListagemTermosAditivosComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ListagemTermosAditivosComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
