import { async, ComponentFixture, TestBed } from '@angular/core/testing';

/* 
TODO corrigir teste ContratoAnexoComponent
describe('ContratoAnexoComponent', () => {
  let component: ContratoAnexoComponent;
  let fixture: ComponentFixture<ContratoAnexoComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ContratoAnexoComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ContratoAnexoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
