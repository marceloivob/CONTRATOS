/* 
TODO corrigir teste IntegrationGuard
describe('IntegrationGuard', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [IntegrationGuard]
    });
  });

  it('should ...', inject([IntegrationGuard], (guard: IntegrationGuard) => {
    expect(guard).toBeTruthy();
  }));
}); */
